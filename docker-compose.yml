services:
  mrjobs:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./profile.yaml:/app/profile.yaml
      - ./resumes:/app/resumes
      - ./data:/app/data
      - ./.cache:/app/.cache
    environment:
      - MRJOBS_DB_PATH=/app/data/applications.db
      - CLAUDE_AUTH_TOKEN=${CLAUDE_AUTH_TOKEN}
    restart: unless-stopped
